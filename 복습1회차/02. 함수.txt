1. 기본 규칙
// 전역변수(상대적 개념)
function 함수명(매개변수, ... ) {
	// 지역 변수 
	return 반환값
}

자바스크립트에서 함수 
1급 함수
1) 함수를 인수(매개변수)로 사용 가능 
2) 함수를 반환값으로 사용 가능
3) 생성자....


el.addEventListener("click", function(e) {

});