객체 - Object
				사물, 대상 
	
		각 대상간, 사물간 상호작용
		
강사 
	- 지식 - 특성   -- 속성, 변수
	- 가르친다 - 행위  -- 함수, 메서드
	
학생 
	- 배우고자 하는 열정 - 특성 
	- 배우고있다           - 행위 
	
객체 - 속성과 메서드로 구성된 자료형

자바스크립트에서 객체를 정의하는 방법
{
	속성 : 값,
	속성 : 값,
	속성 : 값, 
}

값 - 원시타입 값(숫자, 문자, null, undefined) + 객체(+ 함수 객체)


객체의 속성을 접근하는 방법
 - 마침표 연산자(.)